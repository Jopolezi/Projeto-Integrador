import React from "react";
import "./styledProfile"
import { useProfile } from "../../utils/useProfile"

function Profile() {

    const { perfil, loading, updateProfile } = useProfile()

    return (
        <>
        
        </>
    )
}

export default Profile